// Why Enums are being Used.. It is use when ypou want to restrict some user chhoices.

enum SeatChoice {
    AISLE="AISLE",  //magic->hover on AISLE
    MIDDLE=3,
    WINDOW,
    FOURTH
}

const hcSeat = SeatChoice.AISLE